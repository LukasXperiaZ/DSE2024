export class Config {
  // If this variable is set to false, it can be used for deployment to GKE.
  // If this variable is set to true, it can be used for local development.
  static readonly localDevelopment: boolean = false;
}
